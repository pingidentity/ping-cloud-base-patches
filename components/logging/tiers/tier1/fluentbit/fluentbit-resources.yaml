apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluent-bit
spec:
  template:
    spec:
      containers:
        - name: fluent-bit
          resources:
            limits:
              cpu: "500m"
              memory: 2Gi
            requests:
              cpu: "200m"
              memory: 500Mi
