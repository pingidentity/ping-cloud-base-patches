apiVersion: batch/v1
kind: CronJob
metadata:
  name: pingaccess-cert-validation
  namespace: ping-cloud
spec:
  schedule: "*/3 * * * *"        # Run every time min.
  concurrencyPolicy: Forbid       # If still running, skip this run. We don't want multiple pods running.
  startingDeadlineSeconds: 300
  suspend: false
  successfulJobsHistoryLimit: 0
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 0             # Don't retry failed jobs
      template:
        spec:
          serviceAccountName: pingaccess-cert-maintainer
          restartPolicy: Never
          containers:
            - name: runner
              image: public.ecr.aws/r2h3l6e4/pingcloud-apps/pingaccess:v2.1.0
              command: ["/bin/sh", "-c"]
              args:
                - /scripts/run.sh
              env:
                - name: STS
                  value: pingaccess
                - name: NAMESPACE
                  value: ping-cloud
                - name: KUBECTL_VERSION
                  value: v1.30.4
              resources:
                requests:
                  cpu: 100m
                  memory: 256Mi
                limits:
                  cpu: 100m
                  memory: 256Mi
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
          volumes:
            - name: scripts
              configMap:
                name: pingaccess-verify-certificate
                defaultMode: 0755