# AI Generated
name: Mirror to pingidentity/ping-cloud-base-patches

on: push

jobs:
  mirror-to-ping-cloud-base-patches:
    uses: ping-internal/p1as-eng-tools/.github/workflows/mirror-repository.yml@PDO-11873-gh-migration
    with:
      ref-name: ${{ github.ref_name }}
      target-repo-ssh-url: "git@github.com:pingidentity/ping-cloud-base-patches.git"
    secrets:
      mirror-deploy-ssh-key: ${{ secrets.MIRROR_DEPLOY_SSH_KEY }}
